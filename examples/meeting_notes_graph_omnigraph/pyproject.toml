[project]
name = "meeting-notes-graph-omnigraph"
version = "0.1.0"
description = "CocoIndex example: build a knowledge graph from meeting notes in Google Drive, stored in Omnigraph."
requires-python = ">=3.11"
dependencies = [
    "cocoindex[google_drive,sentence_transformers,entity_resolution_llm]>=1.0.7",
    "instructor",
    "litellm",
    "pydantic",
]

[tool.setuptools]
py-modules = ["main"]
